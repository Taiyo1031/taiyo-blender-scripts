# Legacy Single File Add-ons

このフォルダには、旧形式の単体 `.py` インストール用アドオンを保管しています。

現在の通常運用では、Blender 4.2以降の Remote Repository 用パッケージを `_Taiyo_Blender_Extensions_Repo` で管理し、`docs/extensions` へ配布zipと `index.json` を出力します。

このフォルダ内のファイルは、過去の手動インストール互換や参照用です。Remote Repository配布には直接使いません。
